# Copy to .env in this folder (docker compose reads .env from its own directory).
# mongodb_exporter needs a user with at least the clusterMonitor role.
ATLAS_URI=mongodb+srv://<user>:<password>@<cluster-host>/?appName=ops-dashboard
